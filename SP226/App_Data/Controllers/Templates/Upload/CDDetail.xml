<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE import [
  <!ENTITY CheckImportUserDefinedFields SYSTEM "..\..\Include\Command\CheckImportUserDefinedFields.txt">
]>

<import xmlns="urn:schemas-fast-com:data-import">
  <setting>
    <startRow value="6"/>
    <maxFileSize value="10"/>
    <uploadTimeOut value="120"/>
    <importRecordTimeout value="1740"/>
    <allowFileExtension value="^(.XLSX)$"/>
    <onProcessFail value="parent.on$CDDetailImportForm$Fail(this.frameElement)"/>
    <onProcessComplete value="parent.on$CDDetailImportForm$Complete(this.frameElement)"/>
    <uploadContentType value="application/ms-excel;application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"/>
    <baseTable value="d51$000000"/>
  </setting>

  <fields identity="true" name="stt">
    <field name="tk_no" column="A" allowNulls="false" upperCase="true"/>
    <field name="ma_kh_i" column="C" upperCase="true"/>
    <field name="tien_nt" column="E" type="Decimal"/>
    <field name="dien_giai" column="F"/>
    <field name="ma_vv" column="G" upperCase="true"/>
    <field name="ma_bp" column="H" upperCase="true"/>
    <field name="so_lsx" column="I" upperCase="true"/>
    <field name="ma_sp" column="J" upperCase="true"/>
    <field name="ma_hd" column="K" upperCase="true"/>
    <field name="ma_phi" column="L" upperCase="true"/>
    <field name="ma_ku" column="M" upperCase="true"/>
  </fields>

  <processing>
    <text>
      <![CDATA[
declare @q nvarchar(4000), @user int, @admin bit, @language char(1), @sys varchar(32), @m int
select @m = character_maximum_length from information_schema.columns where table_name = 'phlsx' and column_name = 'so_lsx'
select @admin = @@admin, @user = @@userID, @language = @@language, @sys = '@@sysDatabaseName'

update @@table set so_lsx = space(@m - len(rtrim(so_lsx))) + rtrim(so_lsx)

create table #r(tk_no varchar(32), ten_tk_no nvarchar(512), ma_kh_i varchar(32), ten_kh_i nvarchar(512), tien_nt numeric(18, 4), dien_giai nvarchar(512),
  ma_vv varchar(32), ma_bp varchar(32), so_lsx varchar(32), ma_sp varchar(32), ma_hd varchar(32), ma_phi varchar(32), ma_ku varchar(32), ref varchar(32), stt int)

select @q = 'insert into #r(tk_no, ten_tk_no, ma_kh_i, ten_kh_i, tien_nt, dien_giai, ma_vv, ma_bp, so_lsx, ma_sp, ma_hd, ma_phi, ma_ku, ref, stt)'
select @q = @q + ' select a.tk_no, case when b.loai_tk = 1 then b.ten_tk%l else null end, a.ma_kh_i, c.ten_kh%l, '
select @q = @q + ' tien_nt, dien_giai, ma_vv, ma_bp, so_lsx, ma_sp, ma_hd, ma_phi, ma_ku, '''', a.stt'
select @q = @q + ' from @@table a left join dmtk b on a.tk_no = b.tk left join dmkh c on a.ma_kh_i = c.ma_kh'
exec sp_executesql @q

]]>&CheckImportUserDefinedFields;<![CDATA[

select '' as message, 'this.frameElement.result = @@result;parent.set$Grid$ImportResult(this.frameElement);' as script
select tk_no, ten_tk_no, ma_kh_i, ten_kh_i, tien_nt, dien_giai, ma_vv, ma_bp, so_lsx, ma_sp, ma_hd, ma_phi, ma_ku, ref from #r order by stt
return
]]>
    </text>
  </processing>

</import>