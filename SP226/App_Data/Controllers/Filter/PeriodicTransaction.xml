<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE dir [
  <!ENTITY CommandCheckLockedDate SYSTEM "..\Include\Command\ProcessLockedDate4Unit.txt">
  <!ENTITY XMLWhenFilterLoading SYSTEM "..\Include\XML\WhenFilterLoading.xml">
  <!ENTITY XMLWhenFilterClosing SYSTEM "..\Include\XML\WhenFilterClosing.xml">
  <!ENTITY ScriptQueryFilterString SYSTEM "..\Include\Javascript\QueryFilterString.txt">
  <!ENTITY OutlineEntry SYSTEM "..\Include\Javascript\OutlineEntry.txt">
]>

<dir xmlns="urn:schemas-fast-com:data-dir">
  <title v="Bút toán phân bổ định kỳ" e="Periodic Transaction"></title>
  <fields>
    <field name="ky" type="Decimal" dataFormatString="#0" allowNulls="false" align="right" aliasName="Period" defaultValue="(new Date()).getMonth()+1">
      <header v="Kỳ" e="Period"></header>
      <items style="Numeric"/>
    </field>
    <field name="nam" type="Decimal" dataFormatString="###0" allowNulls="false" aliasName="Year" defaultValue="(new Date()).getFullYear()" allowContain="true">
      <header v="Năm" e="Year"></header>
      <items style="Numeric"/>
    </field>
    <field name="ma_dvcs">
      <header v="Đơn vị" e="Unit(s)"></header>
      <items style="Lookup" controller="Unit" key="((@@admin = 1) or (ma_dvcs in (select ma_dvcs from sysunitrights where user_id = @@userID and (r_new = 1 or r_edit = 1 or r_del = 1 or r_access = 1)))) and status = '1'" check="((@@admin = 1) or (ma_dvcs in (select ma_dvcs from sysunitrights where user_id = @@userID and (r_new = 1 or r_edit = 1 or r_del = 1 or r_access = 1))))"/>
    </field>
    <field name="keys" hidden="true" inactivate="true" filterSource="Infinite">
      <header v="" e=""></header>
    </field>
  </fields>

  <views>
    <view id="Dir">
      <item value="120, 40, 60, 100, 100, 130, 0"/>
      <item value="11----1: [ky].Label, [ky], [keys]"/>
      <item value="11---: [nam].Label, [nam]"/>
      <item value="11000: [ma_dvcs].Label, [ma_dvcs]"/>
    </view>
  </views>

  <commands>
    &XMLWhenFilterLoading;
    &XMLWhenFilterClosing;
    <command event="Inserting">
      <text>
        &CommandCheckLockedDate;
        <![CDATA[exec rs_Convert2AllocPeriod @ky, @nam, @ma_dvcs, @@language, @@userID, @@admin, 1]]>
      </text>
    </command>

  </commands>

  <script>
    <text>
      &OutlineEntry;
      <![CDATA[/* <flatten type="Javascript"> */
function active$VoucherFilter$(sender) {
  sender.add_onResponseComplete(on$Filter$ResponseComplete);
  changeLookupReadonly(sender, 'ma_dvcs', true);
}
function close$VoucherFilter$(sender) {try {sender.remove_onResponseComplete(on$Filter$ResponseComplete);} catch (ex) {}}
function on$Filter$ResponseComplete(sender, e) {
  var f = e.object, context = e.type.Context, result = e.type.Result;
  switch (context) {
    case 'Checking':
      var g = f.grid, k = [];
      g._p = f.getItemValue('ky'), g._y = f.getItemValue('nam'), g._u = f.getItemValue('ma_dvcs');
      g._alterTitle = [null, [['%s1', $func.trim(g._p.toString()), true], ['%s2', $func.trim(g._y.toString()), true]]];

      Array.add(k, {Name: 'ky', Opr: '=', Value: g._p, Type: 'Decimal', Ignore: false});
      Array.add(k, {Name: 'nam', Opr: '=', Value: g._y, Type: 'Decimal', Ignore: false});
      if (f.getItemValue('ma_dvcs')) Array.add(k, {Name: "dbo.ff_InList(ma_dvcs,'" + g._u + "')", Opr: '=', Value: 1, Type: 'String', Ignore: false});
      g.set_externalKey(k);
      g._queryFilterString = getQueryFilterString(f, 'ky, nam');
      g._$k = g._$c = g._$r = [];
      break;
    default:
      break;
  }
}
function getQueryFilterString(f, s) {
  var q = '', a = s.split(',');
  for (var i = 0; i < a.length; i++) {
    var c = $func.trim(a[i]), o = f.getItem(c), v = (o ? f.getItemValue(c) : c);
    q += String.fromCharCode(253);
    if (o) {
      switch (o.field.Type) {
        case 'DateTime':
          q += $func.formatDateOrder(v);
          break;
        case 'Boolean':
          q += (v ? '1' : '0');
          break;
        case 'Decimal';
          q += v.toString();
          break;
        default:
          q += $func.replace(v, '\'', String.fromCharCode(252));
          break;
      }
    } else q += $func.replace(v, '\'', String.fromCharCode(252));
  }
  return q;
}
/* </flatten> */]]>
    </text>
  </script>

</dir>