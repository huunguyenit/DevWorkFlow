<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE import [
  <!ENTITY IrregularValue SYSTEM "Include\Irregular.txt">

  <!ENTITY Error "
if @r is not null begin
  select '' as message, @field as field, @r as record
  return
end
">
  <!ENTITY Irregular "
if @r is not null begin
  select @irregular as message, @field as field, @r as record
  return
end
">
  <!ENTITY Duplicate "
if @r is not null begin
  select @duplicate as message, @field as field, @r as record
  return
end
">

  <!ENTITY Checking "@@checking">
  <!ENTITY Inserting "@@inserting">
  <!ENTITY Updating "@@updating">
]>

<import xmlns="urn:schemas-fast-com:data-import">
  <setting>
    <startRow value="6"/>
    <maxFileSize value="10"/>
    <uploadTimeOut value="120"/>
    <importRecordTimeout value="1740"/>
    <allowFileExtension value="^(.XLSX)$"/>
    <onProcessFail value="parent.on$SlowMovingItemPhysicalImportForm$Fail(this.frameElement)"/>
    <onProcessComplete value="parent.on$SlowMovingItemPhysicalImportForm$Complete(this.frameElement)"/>
    <uploadContentType value="application/ms-excel;application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
    <baseTable value="cdvtlc2"/>
    <table value="cdvtlc2" alias="a"/>
    <temporary value="#k" alias="b"/>
  </setting>

  <fields identity="true" name="stt">
    <field name="ma_vt" column="A" isPrimaryKey="true" allowNulls="false" upperCase="true" updateValue="None" check="ma_vt not in (select ma_vt from dmvt)"/>
    <field name="ngay_nhap" column="C" type="DateTime" check="ngay_nhap is null and ngay_xuat is null"/>
    <field name="ngay_xuat" column="D" type="DateTime"/>

    <field name="nam" column="None" insertValue="@y" updateValue="None"/>
    <field name="status" column="None" insertValue="'1'" updateValue="None"/>
    <field name="datetime0" column="None" type="DateTime" insertValue="getdate()" updateValue="None"/>
    <field name="datetime2" column="None" type="DateTime" insertValue="getdate()" updateValue="getdate()"/>
    <field name="user_id0" column="None" type="Int" insertValue="@@userID" updateValue="None"/>
    <field name="user_id2" column="None" type="Int" insertValue="@@userID" updateValue="@@userID"/>
  </fields>

  <processing>
    <text>
      <![CDATA[
if @@admin = 0 and @type = '1' begin
  if @@sysDatabaseName.dbo.FastBusiness$System$GetAuthorize(@@admin, @@userID, 'SlowMovingItemPhysical', 'Edit') = 0 select @type = '0'
end

declare @field varchar(32), @r int, @d datetime, @y int

select @d = getDate(), @y = nam_bd from dmstt

-- Delete
if @type = '0' delete @@table from @@table a join cdvtlc2 b on b.nam = @y and a.ma_vt = b.ma_vt

-- Checking
]]>&Checking;<![CDATA[

-- Gather
select a.* into #k from @@table a join cdvtlc2 b on b.nam = @y and a.ma_vt = b.ma_vt
delete @@table from @@table a join #k b on a.ma_vt = b.ma_vt

-- Insert
]]>&Inserting;<![CDATA[

-- Update
if @type = '1' update cdvtlc2 set @@list from cdvtlc2 a, #k b where a.nam = @y and a.ma_vt = b.ma_vt
]]>
    </text>
  </processing>
</import>