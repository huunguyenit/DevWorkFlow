# 18 — Performance Review

---

**Prompt:**

Rà soát hiệu năng: **<module/tính năng>**

Checklist đầy đủ: `prompts/Cursor/18-PERFORMANCE.md` (dùng chung cho mọi AI tool).
Nếu có thể, viết một micro-benchmark nhỏ (hoặc dùng `dotnet-trace`/`BenchmarkDotNet` nếu đã
có sẵn trong repo) để đo trước/sau thay vì chỉ ước lượng bằng mắt.
