<UserControl x:Class="UI.Views.Controls.WorkspaceHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:UI.ViewModels"
             xmlns:pages="clr-namespace:UI.Views.Pages">

    <UserControl.Resources>
        <!-- DataTemplate map: ViewModel → View -->
        <DataTemplate DataType="{x:Type vm:FormBuilderViewModel}" x:Shared="False">
            <pages:FormBuilderPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SqlDocumentViewModel}" x:Shared="False">
            <pages:SqlStudioPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TemplateManagerViewModel}">
            <pages:TemplateManagerPage/>
        </DataTemplate>
    </UserControl.Resources>

    <Border Background="{StaticResource WorkspaceBgBrush}">
        <Grid>
            <TextBlock Text="Open a file from Explorer to start."
                       Style="{StaticResource IdePlaceholderTextStyle}"
                       Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}"/>
            <ContentControl Content="{Binding}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"/>
        </Grid>
    </Border>
</UserControl>
