<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE grid [
  <!ENTITY DowloadScript SYSTEM "..\Include\Javascript\DownloadScript.txt">

  <!ENTITY TransferID "ApprovalIndicator">
  <!ENTITY CreateTicket "declare @ticket varchar(32), @filename varchar(128), @description nvarchar(128)
select @ticket = lower(replace(newid(),'-',''))
if @@language = 'v' select @filename = 'ApprovalIndicator.xlsx', @description = N'Khai báo trạng thái duyệt'
else select @filename = 'ApprovalIndicator2.xlsx', @description = N'Approval Indicator'
insert into @@sysDatabaseName..ticket values(@ticket, @@userID, '&TransferID;', @filename, @description, '@@appDatabaseName', getdate());">
]>

<grid table="dmttcttg" code="loai_duyet, u_status" order="loai_duyet, u_status" filter="xtype = 'U'" xmlns="urn:schemas-fast-com:data-grid">
  <title v="Khai báo trạng thái duyệt" e="Approval Indicator"></title>
  <subTitle v="Loại duyệt: %c..." e="Approval Indicator: %c..."></subTitle>

  <fields>
    <field name="loai_duyet" isPrimaryKey="true" hidden="true" width="0">
      <header v="" e=""></header>
    </field>
    <field name="u_status" isPrimaryKey="true" width="100" allowSorting="true" allowFilter="true">
      <header v="Mã trạng thái duyệt" e="Code"></header>
    </field>
    <field name="u_status_name%l" width="300" allowSorting="true" allowFilter="true">
      <header v="Tên trạng thái duyệt" e="Description"></header>
    </field>
  </fields>

  <views>
    <view id="Grid">
      <field name="loai_duyet"/>
      <field name="u_status"/>
      <field name="u_status_name%l"/>
    </view>
  </views>
  <commands>
    <command event="Loading">
      <text>
        &CreateTicket;<![CDATA[
select 'this._authorize = ' + rtrim(@@sysDatabaseName.dbo.FastBusiness$System$GetAuthorize(@@admin, @@userID, 'ApprovalIndicator', 'New')) + ';this._key = ''' + @ticket + ''';load$GridApprovalIndicator(this);' as message
return
]]>
      </text>
    </command>

    <command event="Closing">
      <text>
        <![CDATA[select 'dispose$GridApprovalIndicator(this);' as message
return]]>
      </text>
    </command>
  </commands>

  <script>
    <text>
      <![CDATA[
function load$GridApprovalIndicator(g) {
  g.add_onResponseComplete(on$GridApprovalIndicator$ResponseComplete);  
  g.add_commandEvent(on$GridApprovalIndicator$ExecuteCommand);
}
function dispose$GridApprovalIndicator(g) {
  try {g.remove_commandEvent(on$GridApprovalIndicator$ExecuteCommand);} catch (ex) {}
  try {g.remove_onResponseComplete(on$GridApprovalIndicator$ResponseComplete);} catch (ex) {}  
}
function on$GridApprovalIndicator$ExecuteCommand(sender, e) {
  var action = e.type.Action, g = sender;
  switch (action) {
    case 'ImportData':
      show$Form(g, 'ApprovalIndicatorImport');
      break;
    case 'Download':
      request$Download(g, action);
      break;
    default:
      break;
  }
}
function on$GridApprovalIndicator$ResponseComplete(sender, e) {
  var g = e.object, context = e.type.Context, result = e.type.Result, o = e.type.Object;
  switch (context) {
    case 'Download':
      g._key = result[0].Value;
      break;
    default:
      break;
  }
}
function show$Form(g, c) {(g._authorize == 1) ? g.showForm(c) : $message.show($df.getResources(g._language, "Message.NotAccess"));}
]]>
      &DowloadScript;
    </text>
  </script>

  <css>
    <text>
      <![CDATA[
div.ImportData{background-image:url(../images/Upload.png);background-repeat:no-repeat;background-position:0 0;}
div.ImportDataOverGreen{background-position:0 -22px;}
div.Download{background-image:url(../Images/Download.png);background-repeat:no-repeat;background-position:0 0;}
div.DownloadOverGreen{background-position:0 -22px;}
]]>
    </text>
  </css>

  <response>
    <action id="Download">
      <text>
        &CreateTicket;<![CDATA[
select @ticket as value
return
]]>
      </text>
    </action>
  </response>

  <toolbar>
    <button command="New">
      <title v="Toolbar.New" e="Toolbar.New"></title>
    </button>
    <button command="Edit">
      <title v="Toolbar.Edit" e="Toolbar.Edit"></title>
    </button>
    <button command="Delete">
      <title v="Toolbar.Delete" e="Toolbar.Delete"></title>
    </button>
    <button command="Search">
      <title v="Toolbar.Search" e="Toolbar.Search"></title>
    </button>
    <button command="View">
      <title v="Toolbar.View" e="Toolbar.View"></title>
    </button>
    <button command="Export">
      <title v="Toolbar.Export" e="Toolbar.Export"/>
    </button>

    <button command="-">
      <title v="-" e="-"/>
    </button>

    <button command="ImportData">
      <title v="Lấy dữ liệu từ tệp..." e="Import Data from File..."></title>
    </button>
    <button command="Download">
      <title v="Tải tệp mẫu..." e="Download Template File..."/>
    </button>

    <button command="Separate">
      <title v="-" e="-"/>
    </button>
    <button command="Freeze">
      <title v="Toolbar.Freeze" e="Toolbar.Freeze"></title>
    </button>
  </toolbar>
</grid>