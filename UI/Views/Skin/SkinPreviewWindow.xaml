<Window x:Class="UI.Views.Skin.SkinPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Xem skin — DevWorkFlow"
        Width="1100" Height="760"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="InfoText" Margin="10,6" Foreground="#57606A"
                   Text="Skin trống (chrome-only) — vùng host Designer để trống, chưa nhúng form."/>
        <wv2:WebView2 Grid.Row="1" x:Name="PreviewWebView" Margin="10,0,10,10"/>
    </Grid>
</Window>
