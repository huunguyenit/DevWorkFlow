<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE import [
  <!ENTITY IrregularValue SYSTEM "Include\Irregular.txt">

  <!ENTITY Error "
if @r is not null begin
  select '' as message, @field as field, @r as record
  return
end
">
  <!ENTITY Irregular "
if @r is not null begin
  select @irregular as message, @field as field, @r as record
  return
end
">
  <!ENTITY Duplicate "
if @r is not null begin
  select @duplicate as message, @field as field, @r as record
  return
end
">
  <!ENTITY Checking "@@checking">
  <!ENTITY Inserting "@@inserting">
  <!ENTITY Updating "@@updating">
]>

<import xmlns="urn:schemas-fast-com:data-import">
  <setting>
    <startRow value="6"/>
    <maxFileSize value="10"/>
    <uploadTimeOut value="120"/>
    <importRecordTimeout value="1740"/>
    <allowFileExtension value="^(.XLSX)$"/>
    <onProcessFail value="parent.on$TSGroupImportForm$Fail(this.frameElement)"/>
    <onProcessComplete value="parent.on$TSGroupImportForm$Complete(this.frameElement)"/>
    <uploadContentType value="application/ms-excel;application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
    <baseTable value="dmnhcc"/>
    <table value="dmnhcc" alias="a"/>
    <temporary value="#k" alias="b"/>
  </setting>

  <fields identity="true" name="stt">
    <field name="loai_nh" column="A" isPrimaryKey="true" type="Decimal" updateValue="None" defaultValue="1" check="loai_nh not in ('1', '2', '3')"/>
    <field name="ma_nh" column="B" isPrimaryKey="true" allowNulls="false" upperCase="true"/>
    <field name="ten_nh" column="C" allowNulls="false"/>
    <field name="ten_nh2" column="D"/>

    <field name="status" column="None" insertValue="'1'" updateValue="None"/>
    <field name="datetime0" column="None" type="DateTime" insertValue="getdate()" updateValue="None"/>
    <field name="datetime2" column="None" type="DateTime" insertValue="getdate()" updateValue="getdate()"/>
    <field name="user_id0" column="None" type="Int" insertValue="@@userID" updateValue="None"/>
    <field name="user_id2" column="None" type="Int" insertValue="@@userID" updateValue="@@userID"/>
  </fields>

  <processing>
    <text>
      <![CDATA[
if @@admin = 0 and @type = '1' begin
  if @@sysDatabaseName.dbo.FastBusiness$System$GetAuthorize(@@admin, @@userID, 'TSGroup', 'Edit') = 0 select @type = '0'
end

declare @q nvarchar(4000), @duplicate nvarchar(4000), @irregular nvarchar(4000), @irregularChars varchar(128), @field varchar(32), @r int

select @irregularChars = ]]>&IrregularValue;<![CDATA[
select @irregular = case @@language when 'v' then N'Giá trị tại ô <span class="Highlight">%invalidCell</span> có chứa các ký tự: ' + @irregularChars else 'The value of cell <span class="Highlight">%invalidCell</span> contains any of the following characters: ' + @irregularChars  end
select @duplicate = case @@language when 'v' then N'Giá trị tại ô <span class="Highlight">%invalidCell</span> đã có hoặc lồng nhau.' else 'The value of cell <span class="Highlight">%invalidCell</span> is invalid or already exists.' end

create index i on @@table (loai_nh, ma_nh)

-- Delete
if @type = '0' delete @@table from @@table a join dmnhcc b on a.loai_nh = b.loai_nh and a.ma_nh = b.ma_nh

-- Checking
select @field = 'ma_nh', @r = min(stt) from @@table where ma_nh like '%[' + @irregularChars + ']%']]>&Irregular;<![CDATA[

-- Checking
]]>&Checking;<![CDATA[

-- Gather
select a.* into #k from @@table a join dmnhcc b on a.loai_nh = b.loai_nh and a.ma_nh = b.ma_nh
delete @@table from @@table a join #k b on a.loai_nh = b.loai_nh and a.ma_nh = b.ma_nh

-- Checking
select @field = 'ma_nh'
select @q = 'select top 1 @r = a.stt from @@table a, @@table b with (index(i)) where a.loai_nh = b.loai_nh and a.ma_nh <> b.ma_nh and a.ma_nh like rtrim(b.ma_nh) + ''%'''
exec sp_executesql @q, N'@r int output', @r = @r output]]>&Duplicate;<![CDATA[
select top 1 @r = a.stt from @@table a, dmnhcc b with (index(PK_dmnhcc)) where a.loai_nh = b.loai_nh and a.ma_nh like rtrim(b.ma_nh) + '%']]>&Duplicate;<![CDATA[
select @q = 'select top 1 @r = b.stt from dmnhcc a, @@table b with (index(i)) where a.loai_nh = b.loai_nh and a.ma_nh like rtrim(b.ma_nh) + ''%'''
exec sp_executesql @q, N'@r int output', @r = @r output]]>&Duplicate;<![CDATA[

-- Insert
]]>&Inserting;<![CDATA[
-- Update
if @type = '1' begin ]]>&Updating;<![CDATA[ end
]]>
    </text>
  </processing>
</import>