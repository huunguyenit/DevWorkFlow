# Specs — Database

Liên quan: `01-ARCHITECTURE.md` (SQL Server + Dapper), `04-TREE_FRAMEWORK.md` (DatabaseDataSource),
`05-WORKFLOW_ENGINE.md` (Database Actions).

Lưu ý: UI không được truy cập Database trực tiếp (Rule R2) — mọi spec ở đây phải mô tả luồng đi
qua Service/Provider tương ứng, không phải UI gọi thẳng SQL.

Ví dụ feature có thể cần spec: `database-explorer-tree.md`, `execute-sql-action.md`,
`connect-database.md`.

Chưa có spec nào.
