<Window x:Class="UI.Views.Skin.SkinCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:skin="clr-namespace:UI.Views.Skin"
        Title="Lấy skin — DevWorkFlow"
        Width="1100" Height="760"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10,8" LastChildFill="True">
            <TextBlock DockPanel.Dock="Left" Text="Base URL:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Button DockPanel.Dock="Right" x:Name="OpenButton" Content="Mở" Width="70" Margin="8,0,0,0"
                    Click="OpenButton_Click"/>
            <TextBox x:Name="UrlBox" VerticalContentAlignment="Center" Padding="4,2"
                     KeyDown="UrlBox_KeyDown"/>
        </DockPanel>

        <skin:WebView2SkinCaptureHost Grid.Row="1" x:Name="CaptureHost" Margin="10,0"/>

        <DockPanel Grid.Row="2" Margin="10,8">
            <TextBlock x:Name="HintText" DockPanel.Dock="Left" VerticalAlignment="Center"
                       Foreground="#57606A"
                       Text="Đăng nhập site ERP, mở tới trang shell, rồi bấm &quot;Dùng trang này&quot;."/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="UsePageButton" Content="Dùng trang này" Width="130" Margin="0,0,8,0"
                        IsEnabled="False" Click="UsePageButton_Click"/>
                <Button x:Name="CancelButton" Content="Huỷ" Width="80" Click="CancelButton_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
