<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE grid [
  <!ENTITY DowloadScript SYSTEM "..\Include\Javascript\DownloadScript.txt">

  <!ENTITY TransferID "PaymentTerm">
  <!ENTITY CreateTicket "declare @ticket varchar(32), @filename varchar(128), @description nvarchar(128)
select @ticket = lower(replace(newid(),'-',''))
if @@language = 'v' select @filename = 'PaymentTerm.xlsx', @description = N'Danh mục thanh toán'
else select @filename = 'PaymentTerm2.xlsx', @description = N'Payment Term List'
insert into @@sysDatabaseName..ticket values(@ticket, @@userID, '&TransferID;', @filename, @description, '@@appDatabaseName', getdate());">
]>
<grid table="dmtt" code="ma_tt" order="ma_tt" xmlns="urn:schemas-fast-com:data-grid">

  <title v="Danh mục thanh toán" e="Payment Term List"></title>
  <subTitle v="Cập nhật thanh toán: thêm, sửa, xóa..." e="Add, Edit, Delete Payment Term..."></subTitle>

  <fields>
    <field name="ma_tt" isPrimaryKey="true" width="100" dataFormatString="X" allowSorting="true" allowFilter="true">
      <header v="Mã thanh toán" e="Payment Term"></header>
    </field>
    <field name="ten_tt%l" width="300" allowSorting="true" allowFilter="true">
      <header v="Tên thanh toán" e="Description"></header>
    </field>
    <field name="han_tt" width="120" allowSorting="true" allowFilter="true" align="right">
      <header v="Hạn thanh toán" e="Payment Day(s)"></header>
    </field>
  </fields>

  <views>
    <view id="Grid">
      <field name="ma_tt"/>
      <field name="ten_tt%l"/>
      <field name="han_tt"/>
    </view>
  </views>


  <commands>
    <command event="Loading">
      <text>
        &CreateTicket;<![CDATA[
select 'this._authorize = ' + rtrim(@@sysDatabaseName.dbo.FastBusiness$System$GetAuthorize(@@admin, @@userID, 'PaymentTerm', 'New')) + ';this._key = ''' + @ticket + ''';load$GridPaymentTerm(this);' as message
return
]]>
      </text>
    </command>

    <command event="Closing">
      <text>
        <![CDATA[select 'dispose$GridPaymentTerm(this);' as message
return]]>
      </text>
    </command>
  </commands>

  <script>
    <text>
      <![CDATA[
function load$GridPaymentTerm(g) {
  g.add_onResponseComplete(on$GridPaymentTerm$ResponseComplete);  
  g.add_commandEvent(on$GridPaymentTerm$ExecuteCommand);
}
function dispose$GridPaymentTerm(g) {
  try {g.remove_commandEvent(on$GridPaymentTerm$ExecuteCommand);} catch (ex) {}
  try {g.remove_onResponseComplete(on$GridPaymentTerm$ResponseComplete);} catch (ex) {}  
}
function on$GridPaymentTerm$ExecuteCommand(sender, e) {
  var action = e.type.Action, g = sender;
  switch (action) {
    case 'ImportData':
      show$Form(g, 'PaymentTermImport');
      break;
    case 'Download':
      request$Download(g, action);
      break;
    default:
      break;
  }
}
function on$GridPaymentTerm$ResponseComplete(sender, e) {
  var g = e.object, context = e.type.Context, result = e.type.Result, o = e.type.Object;
  switch (context) {
    case 'Download':
      g._key = result[0].Value;
      break;
    default:
      break;
  }
}
function show$Form(g, c) {(g._authorize == 1) ? g.showForm(c) : $message.show($df.getResources(g._language, "Message.NotAccess"));}
]]>
      &DowloadScript;
    </text>
  </script>

  <css>
    <text>
      <![CDATA[
div.ImportData{background-image:url(../images/Upload.png);background-repeat:no-repeat;background-position:0 0;}
div.ImportDataOverGreen{background-position:0 -22px;}
div.Download{background-image:url(../Images/Download.png);background-repeat:no-repeat;background-position:0 0;}
div.DownloadOverGreen{background-position:0 -22px;}
]]>
    </text>
  </css>

  <response>
    <action id="Download">
      <text>
        &CreateTicket;<![CDATA[
select @ticket as value
return
]]>
      </text>
    </action>
  </response>

  <toolbar>
    <button command="New">
      <title v="Toolbar.New" e="Toolbar.New"></title>
    </button>
    <button command="Edit">
      <title v="Toolbar.Edit" e="Toolbar.Edit"></title>
    </button>
    <button command="Delete">
      <title v="Toolbar.Delete" e="Toolbar.Delete"></title>
    </button>
    <button command="View">
      <title v="Toolbar.View" e="Toolbar.View"></title>
    </button>
    <button command="Export">
      <title v="Toolbar.Export" e="Toolbar.Export"/>
    </button>

    <button command="-">
      <title v="-" e="-"/>
    </button>

    <button command="ImportData">
      <title v="Lấy dữ liệu từ tệp..." e="Import Data from File..."></title>
    </button>
    <button command="Download">
      <title v="Tải tệp mẫu..." e="Download Template File..."/>
    </button>

    <button command="Separate">
      <title v="-" e="-"/>
    </button>
    <button command="Freeze">
      <title v="Toolbar.Freeze" e="Toolbar.Freeze"></title>
    </button>
  </toolbar>

</grid>